export default function PostsLayout({ children }) {
  return (
    <div style={{ minHeight: "100vh", backgroundColor: "blue", padding: 20 }}>
      <h1>POSTS LAYOUT</h1>
      {children}
    </div>
  );
}
